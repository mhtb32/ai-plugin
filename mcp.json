{
  "mcpServers": {
    "posthog": {
      "type": "http",
      "url": "${POSTHOG_MCP_URL}",
      "headers": {
        "x-posthog-mcp-consumer": "plugin",
        "Authorization": "Bearer ${POSTHOG_MCP_API_KEY}"
      }
    }
  }
}
